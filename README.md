**If you want to build and run this project in your own system:**
1. Have or get "Docker Desktop"
2. "git clone 'reposition-url'"
3. Run "docker-compose up --build"
4. Localhost addresses: client = 3001, management = 3000, backend = 8080
